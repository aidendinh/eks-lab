{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sample-microservices.serviceAccountName" . }}
  labels:
    {{- include "sample-microservices.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
